import Directory from "../../components/directory/directory.components";

const Home = () => {
  return (
    <div className="categories-container">
      <Directory />
    </div>
  );
};

export default Home;
